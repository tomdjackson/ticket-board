This solution was built using NextJS and Typescript.

You can run this project locally by installing dependencies and running:
```npm run dev```

This projected is connected to production aws lamda as per process.env.url